require('dotenv').config();
const axios = require('axios');  async function testPredict() {     const GEMINI_API_KEY = process.env.GEMINI_API_KEY;     const url = `https://generativelanguage.googleapis.com/v1beta/models/imagen-3.0-generate-001:predict?key=${GEMINI_API_KEY}`;          try {         const res = await axios.post(url, {             instances: [{ prompt: "A cute red panda" }],             parameters: { sampleCount: 1, aspectRatio: "1:1" }         });         console.log("¡ÉXITO (Predict)! Imagen recibida.");     } catch (e) {         console.error("ERROR (Predict):", e.response ? e.response.status : e.message);     } }  testPredict();
