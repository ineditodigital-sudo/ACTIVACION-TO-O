require('dotenv').config();
const axios = require('axios'); const GEMINI_API_KEY = process.env.GEMINI_API_KEY; axios.post(`https://generativelanguage.googleapis.com/v1beta/models/imagen-3.0-generate-001:generateImages?key=${GEMINI_API_KEY}`, {   "instances": [{ "prompt": "A cute red panda wearing a chef hat" }],   "parameters": { "sampleCount": 1 } }).then(r => {   console.log("SUCCESS:", r.data); }).catch(e => {   if (e.response) {     console.log("ERROR STATUS:", e.response.status);     console.log("ERROR DATA:", JSON.stringify(e.response.data, null, 2));   } else {     console.log("ERROR MESSAGE:", e.message);   } });
