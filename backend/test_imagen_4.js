require('dotenv').config();
async function testImagen4() {     const key = process.env.GEMINI_API_KEY;     const model = "imagen-4.0-generate-001";     const url = `https://generativelanguage.googleapis.com/v1beta/models/${model}:predict?key=${key}`;      const payload = {         "instances": [             { "prompt": "A cute red panda mascot waving" }         ],         "parameters": {             "sampleCount": 1         }     };      console.log(`Intentando llamar a IMAGEN 4 (${model})...`);      try {         const response = await fetch(url, {             method: 'POST',             headers: { 'Content-Type': 'application/json' },             body: JSON.stringify(payload)         });          const data = await response.json();         console.log("Status:", response.status);          if (response.status === 200) {             console.log("¡ÉXITO TOTAL!");             if (data.predictions && data.predictions[0].bytesBase64Encoded) {                 console.log("Imagen recibida correctamente.");             } else {                 console.log("Respuesta inesperada del modelo:", JSON.stringify(data).substring(0, 500));             }         } else {             console.log("ERROR IMAGEN 4:", JSON.stringify(data, null, 2));         }     } catch (e) {         console.error("Fallo de red:", e.message);     } }  testImagen4();
